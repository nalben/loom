import React from 'react';
import classes from './Product.module.scss';

const Product = () => {
  return (
    <div className={`${classes.Product} border-content-block`}>
      продукт
    </div>
  );
};

export default Product;
